# GEMINI.md - Orchestral Score Optimizer Master Directives

## 1. 最重要ミッション
- プロジェクト直下にある `app.py` のアルゴリズム・挙動を忠実に TypeScript/React へ移植すること。
- ブラウザがフリーズする「処理中」ハングアップを根本解決し、スムーズで安定した動作を担保すること。
- スキャナの黒帯を確実に除去した上で、クロップ矩形を基準とした左右2分割を正しく実行すること。

## 2. フリーズ・クラッシュ絶対防止規約 (Zero Freeze Directives)
1. **プレビューの軽量化**:
   - 画面表示・二値化・黒枠検出は、元画像を縮小した軽量プレビュー（長辺1200px程度、scale: 0.2〜0.25）上で行うこと。巨大な元解像度のままメインスレッドで全ピクセル走査しないこと。
2. **レンダリングの排他制御 (Lock & Cancel)**:
   - レンダリング中に新しいリクエストが来たら、前の処理をキャンセルまたは無視し、`renderTask.promise` が完了するまで次の描画を行わないこと。
   - 必ず `try ... finally` で `isLoading = false` を保証し、ローディングの永久スタックを防ぐこと。
3. **非同期解放**:
   - 重いループ処理では `await new Promise(r => setTimeout(r, 0))` を適宜挟み、ブラウザの描画スレッドをブロックしないこと。

## 3. 幾何・パイプライン順序規約
1. **Step 1: 全体クロップ**: スキャナの黒余白（black_margin_threshold）を除外し、見開き全体を囲む矩形を特定。
2. **Step 2: クロップ枠内の分割**: クロップ矩形の幅（width）を基準に、中央分割線（split_offset_percent）で左右にスプリット。
3. **Step 3: A4配置**: マージン初期値は 0mm（余白なし最大化）とし、縦横比を維持して中央配置。
