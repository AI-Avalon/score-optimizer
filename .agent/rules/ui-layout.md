# Apple HIG UI/UX Rules
- 全画面レイアウトは `h-screen w-screen overflow-hidden`、モバイルは `100dvh` およびセーフエリア制約を厳守すること。横スクロールを絶対に発生させないこと。
- クロップハンドルのタッチ領域は最小 44x44px を確保すること（視覚要素 12px とタッチ領域の分離）。
- 画面上部にカプセル型のズームHUD（[-] [100%] [Fit] [+]）をフロート表示すること。
